# Todo Frontend (React)

This is a modular React (Vite) frontend for the Todo backend.

## Backend
Run the backend at http://localhost:8080.

## Run
```bash
npm install
npm run dev
```

Then open the URL shown by Vite (usually http://localhost:5173).
